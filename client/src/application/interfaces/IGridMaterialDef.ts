import { Color3 } from 'babylonjs';

export interface IGridMaterialDef {
    majorUnitFrequency: number;
    minorUnitVisibility: number;
    gridRatio: number;
    mainColor: Color3;
    lineColor: Color3;
    opacity: number;
}
